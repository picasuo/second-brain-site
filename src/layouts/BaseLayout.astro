---
import { ClientRouter } from "astro:transitions";
import TerminalShell from "./TerminalShell.astro";
import "../styles/global.css";

interface Props {
  title: string;
  ariaLabel: string;
  windowTitle: string;
}

const { title, ariaLabel, windowTitle } = Astro.props;
---

<!doctype html transition:animate="none">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <script is:inline>
      if (window["__secondBrainTerminalInitialForm"] === undefined) {
        window["__secondBrainTerminalInitialForm"] = window.matchMedia("(max-width: 699px)").matches ? "fullscreen" : "windowed";
        document.documentElement.dataset.terminalInitialForm = window["__secondBrainTerminalInitialForm"];
      }
    </script>
    <link rel="icon" type="image/png" sizes="48x48" href="/favicon.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <title>{title}</title>
    <ClientRouter />
  </head>
  <body>
    <main>
      <TerminalShell ariaLabel={ariaLabel} windowTitle={windowTitle}>
        <slot />
        <slot name="footer" slot="footer" />
      </TerminalShell>
    </main>
  </body>
</html>
